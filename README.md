# medical_insurance
this is a basic ML model trained on datapoints affecting the varied ranges of charges(insurance fee). i typically analyzed and worked over this dataset by validating the correctness of data, checking the numerical and categorical columns of the data necessary to train the model and lastly, the skewed distribution of the data in normal curve.
